import { defineStore } from 'pinia'
import StoreId from './storeId'

const useQRecordsOptionStore = defineStore(StoreId.QRECORDS_QPTION, {
    state: () => {
        return {
        }
    },
    actions: {
    }
})

export default useQRecordsOptionStore 